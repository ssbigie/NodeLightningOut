<!DOCTYPE html>
<html lang="en">
  <head>
     <% include ../partials/ldOut %>
  </head>
  <body class="slds">
    <header>
    </header>
    <main>
      <div >
        <p>Chatter Feed Should Show Below</p>
        <div id="chatterFeed"></div>
          </div>
      </main>
        <footer>
        </footer>
        <script>
          var url = "<%= ouathLightningURL %>";
          var token = "Bearer "+"<%= oauthtoken %>";
          console.log(token);
            $Lightning.use("dependencyApp.app", function() {
                  $Lightning.createComponent("forceChatter:feed", {},
                     "chatterFeed",
                      function(cmp) {
                         // do something?
                      });
            },url,token );
        </script>
  </body>
</html>
